# ⚠️  DEPRECATED — This file has been split into:
#
#   frontend.yml  — Frontend CI: lint, format-check, unit, integration, E2E, build
#   backend.yml   — Backend CI:  lint, format-check, API tests (Jest + supertest)
#   pr-checks.yml — PR Gates:    lint + format-check for both client and server
#
# This file is kept only as a reference and is intentionally NOT triggered.
# Delete this file once the team has confirmed the new workflows are working.

name: _DEPRECATED_node-build

on:
  workflow_dispatch: # manual trigger only — not automatic

jobs:
  deprecated-notice:
    name: "⚠️ This workflow is deprecated"
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "This workflow has been replaced."
          echo "See frontend.yml, backend.yml, and pr-checks.yml instead."
          exit 1
